const Schema = require('./db.js')
const mongoose = require('mongoose')

// const User = new Schema({
//   name: String,
//   pwd: String
// })

// const UserModel = mongoose.model('users', User)

const api = {
  // login(params) {
  //   UserModel.find({name: params.name}, (error, data) => {
  //     console.log(data)
  //   }
  // }
}

module.exports = api
